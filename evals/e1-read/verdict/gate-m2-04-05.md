# Verdict — GATE agrupado m2-04 + m2-05

- **Fecha**: 2026-07-17T19:18:03+02:00 · **Adjudicador**: consultor Fable delegado (dispatch fresco, sin participación en ninguna fase de estas piezas)
- **Régimen**: gates firmado por Paul, spec M2 §8; agrupamiento 04+05 pedido por el ledger (presupuesto, no destino — veredictos independientes por rama)
- **Ramas**: `m2-04` (commits `8b1817f`, `32c069e`) y `m2-05` (commits `3f991b6`, `33d2797`), ambas sobre main `f96c1f7`
- **Verificación**: primaria propia — todos los oráculos re-corridos por el adjudicador en los worktrees; los reportes y reviews previos NO se dieron por buenos

## Veredicto

| Rama | Veredicto |
|---|---|
| `m2-04` | **MERGED** |
| `m2-05` | **MERGED** |

---

## 1. Verificación primaria — m2-04 (outputs literales de mis corridas)

**cargo test** (worktree m2-04): exit 0. Suite `indexer.rs`:

```
test link_roto_no_es_error ... ok
test wikilink_a_nota_existente_resuelve_destino_permalink ... ok
test wikilink_con_alias_se_guarda_entero_y_resuelve_por_parte_antes_del_pipe ... ok
test wikilink_duplicado_en_la_misma_nota_no_duplica_fila ... ok
test wikilink_roto_se_cura_solo_cuando_aparece_la_nota_destino ... ok
test reindexar_una_nota_que_pierde_un_link_borra_la_arista_vieja ... ok
test rebuild_doble_da_el_mismo_conteo_de_aristas ... ok
test result: ok. 11 passed; 0 failed; 0 ignored
```

Resto de suites (nota 7/7, schema 2/2, smoke 2/2+1 ignored preexistente, walker 4/4, unit aristas incluidos en lib): todo verde, 0 fallos — **los 22 tests de M2-03 no se rompen**. El test contractual `link_roto_no_es_error` existe con el nombre EXACTO fijado en spec indexer §1.1 fila 6 (cita: comando/test «test M2-04 `link_roto_no_es_error` (a escribir)»).

**`exo rebuild` a DB temporal (scratchpad) sobre la KB real + oráculos**:

```
== rebuild #A ==
{"command":"rebuild","data":{"borradas":0,"indexadas":115,"saltadas":0},"schema_version":1}
exit=0
(473, 19)          # (aristas, destino_permalink NULL)
== rebuild #B ==
{"command":"rebuild","data":{"borradas":0,"indexadas":115,"saltadas":0},"schema_version":1}
exit=0
(473, 19)          # idéntico ⇒ idempotente
== wikilinks-ref --diff ==
ref=473 engine=473 faltan=0 sobran=0
exit=0
== corpus-parity --diff ==
gold=115 engine=115 faltan=0 sobran=0
exit=0
```

**Test negativo del oráculo (mío, no el de la review)**: corrompí a mano una arista (`destino_texto='CORRUPTO-GATE'`) en mi DB scratch y `wikilinks-ref.py --diff` la cazó:

```
FALTA en engine: kb-demo/agents -> Paul - perfil de trabajo
SOBRA en engine: kb-demo/agents -> CORRUPTO-GATE
ref=473 engine=473 faltan=1 sobran=1
```

**Spot-check independiente (mío)**: `learnings/construir-con-llms.md` — 10 wikilinks distintos en el fichero (extracción propia con el patrón) vs 10 filas en `aristas`, todas resueltas a permalinks reales. Coincidencia exacta.

## 2. Verificación primaria — m2-05 (outputs literales de mis corridas)

**cargo test** (worktree m2-05): exit 0. Suite `buscador.rs`:

```
test db_inexistente_da_error_claro ... ok
test query_con_guiones_y_acentos_no_revienta ... ok
test resultados_a_nivel_entidad_con_tipo_fijo ... ok
test limite_recorta_resultados ... ok
test query_sin_hits_es_exito_con_resultados_vacios ... ok
test resultados_ordenados_por_score_descendente ... ok
test envelope_data_serializa_con_las_claves_del_contrato_4_1 ... ok
test result: ok. 7 passed; 0 failed
```

Suites M2-03 (git_epoch 2/2, indexer 4/4, nota 7/7, schema 2/2, smoke 2/2+1 ignored, walker 4/4): todo verde — sin regresiones.

**Rebuild + replay + analyze + parity**:

```
== rebuild m2-05 ==
{"command":"rebuild","data":{"borradas":0,"indexadas":115,"saltadas":0},"schema_version":1}
exit=0
== replay-engine engine-fts ==
[56/56] ... OK → .../results/engine-fts.jsonl    exit=0
56 filas, 0 filas con "error"
== analyze.py engine-fts ==
.../results/metrics-engine-fts.md    exit=0 (sin excepciones)
== corpus-parity --diff ==
gold=115 engine=115 faltan=0 sobran=0
exit=0
```

**Reproducibilidad de los artefactos commiteados** (verificación extra, mía): tras regenerar con mi propia corrida, `metrics-engine-fts.md` quedó **byte a byte idéntico** al commiteado (no apareció en `git diff` — el text 28/55 es determinista). Y comparando las 56 filas del `engine-fts.jsonl` commiteado contra `exo search` en vivo (ignorando solo `elapsed_s`): `queries=56 divergencias=0` — scores, orden y permalinks exactos. El worktree quedó restaurado a su estado commiteado tras mi verificación (`git checkout -- results/engine-fts.jsonl`; status limpio).

## 3. Perímetro (verificado con `git -C <worktree> diff main...<rama> --stat`)

- **m2-04** (7 ficheros): `Cargo.toml`/`Cargo.lock` (+1 línea cada uno, solo la dep `regex`), `aristas.rs` (nuevo), `indexer.rs` (dos llamadas + comentario), `lib.rs` (+1 `pub mod`), `tests/indexer.rs`, `wikilinks-ref.py` (nuevo). **Intactos**: `schema.rs` (DDL sellado §2), `envelope.rs`, `main.rs`, `evals/e1-read/gold/`, harness M0 completo.
- **m2-05** (7 ficheros): `buscador.rs` (nuevo), `lib.rs` (+1 `pub mod`), `main.rs` (subcomando Search), `tests/buscador.rs` (nuevo), `replay-engine.py` (nuevo), `results/engine-fts.jsonl` + `results/metrics-engine-fts.md` (nuevos). **Intactos**: `schema.rs`, `envelope.rs`, `indexer.rs`, `analyze.py`, `replay.py`, `eval.jsonl`, results históricos de bm, `gold/`. Cumple spec M2 §4: «`analyze.py` intacto ... `replay.py` NO se generaliza».
- Leí los diffs completos línea a línea: cero desviaciones no declaradas respecto a reportes y packages.

## 4. Adjudicaciones

### 4.1 Punto de política (m2-05): results del arm commiteados — **RATIFICADO, se quedan en el repo**

`git ls-files evals/retrieval-fase0/results/` en main muestra los arms de bm trackeados (`baseline.jsonl`, `jina-es.jsonl`, `minilm.jsonl`, `textfts.jsonl` + sus `metrics-*.md`): commitear `engine-fts.jsonl` + `metrics-engine-fts.md` es exactamente la convención vigente del repo. El brief lo anticipa: «el único fichero nuevo en `results/` es el de tu arm» — un fichero nuevo en results/ solo tiene sentido como fichero versionado. Refuerzo empírico: el jsonl commiteado reproduce 56/56 exacto módulo `elapsed_s` y el metrics es byte-determinista, así que lo commiteado no es una foto irrecuperable sino un artefacto regenerable. El nit del `elapsed_s` no determinista es preexistente repo-wide (los jsonl de bm lo tienen igual); cambiarlo sería decisión repo-wide, fuera del alcance de m2-05.

### 4.2 Decisiones declaradas de m2-04 — las 4 RATIFICADAS

1. **Módulo propio `aristas.rs`** — ratificada. Deja `extrae_wikilinks`/`resuelve_destinos` testeables sin DB (5 unit tests) y minimiza superficie compartida con m2-05. Coherente con el layout de spec M2 §2 (crate único, módulos por responsabilidad).
2. **`regex = "1.13.1"` dep directa** — ratificada. Verificado en el diff del lock: +1 línea (`"regex"` en la lista de deps de `exo`), cero bumps de versión — ya era transitiva vía `tokenizers`, pinada a la resolución existente.
3. **`ORDER BY permalink` en el mapa título→permalink** — ratificada. Verificado en código: el HashMap se construye del vector ordenado, la última inserción gana ⇒ determinista ante títulos duplicados (hoy 0 colisiones en 115 notas). Sostiene la idempotencia que exige el oráculo (spec indexer §3: «rebuild idempotente (dos rebuilds seguidos ⇒ mismo diff ∅)»).
4. **Spot-check incluyendo la nota "sucia"** — ratificada. Elegir la nota con el falso positivo real en vez de 5 notas limpias es exactamente el espíritu del oráculo de spec M2 §3 («extractor de referencia por script + spot-check»): el spot-check existe para destapar lo que el cross-check circular no puede.

### 4.3 Decisiones declaradas de m2-05 — las 2 RATIFICADAS

1. **Guard de DB inexistente** — ratificada. `rusqlite::Connection::open` crea el fichero si falta; sin el guard, `exo search --db no-existe.db` dejaría un `.db` vacío como side-effect y un error confuso. El guard da error claro a stderr + exit ≠0, alineado con la doctrina del envelope (spec indexer §4, literal): «gate en el exit code del proceso, JAMÁS en campos informativos de `data`». Cubierto por test (`db_inexistente_da_error_claro`, verde en mi corrida).
2. **`--exo` default recalculado** (off-by-one del brief) — ratificada. Verifiqué la aritmética: desde `evals/retrieval-fase0/harness/replay-engine.py`, `parents[2]` = `evals/` — la fórmula literal del brief apuntaría a `<repo>/evals/engine/...`, que no existe. El fix (`BASE.parent.parent / "engine/..."`) resuelve al binario real; mi corrida del replay usó ese default y funcionó (56/56, exit 0). Desviación declarada y documentada en el propio script — cero silenciosas.

### 4.4 Falso positivo `[[:space:]]` (m2-04) — **TOLERABLE, no exige fix**

Verificado primariamente: `log/agent-develop-bitacora.md:47` contiene `make[[:space:]]+test` en código inline; el patrón lo captura como arista `(agent-develop-bitacora, ':space:', NULL)` — confirmé la fila en mi DB scratch. Adjudicación por dos vías escritas: (a) spec indexer §1 regla 6, literal: «Links a notas inexistentes se toleran (to_id NULL), jamás error de indexado» — el schema §2 lo modela («`destino_permalink` NULL si la nota destino no existe») y el comportamiento observado es exactamente ese; (b) el brief pre-adjudicó el YAGNI, literal: «Sin parsing de markdown más listo (código inline, fences): YAGNI — si el spot-check del oráculo destapa falsos positivos reales en la KB, se documentan; no se especula». Se documentó, no se especuló. Además el coste real es cero hoy: spec M2 §1 «ranking por grafo (solo indexado de aristas)» está fuera de E1 — nadie consume `aristas` para retrieval aún. Si M3+ mete ranking por grafo, ahí se revisita con datos (1 falso positivo de 473 aristas = 0.2%).

## 5. Nota para GATE-EXEC (no bloquea, corrige una imprecisión de los packages)

Ambos packages declaran solape «~nulo» en `lib.rs`. Simulé el merge a 3 bandas (`git merge-file`, read-only): el merge secuencial 04→05 produce **un conflicto textual trivial** en `engine/src/lib.rs` — ambas ramas insertan su línea (`pub mod aristas;` / `pub mod buscador;`) en el mismo punto, antes de `pub mod envelope;`. Resolución obvia: conservar ambas líneas en orden alfabético (`aristas`, `buscador`, `envelope`, ...). El primer merge (main+m2-04) es limpio (verificado con `git merge-tree`: exit 0). `main.rs` no conflicta (m2-04 no lo toca).

## 6. Qué busqué para objetar (mandato de disenso, condición 3 del régimen)

Ángulos atacados activamente, encontrara algo o no:

1. **Tests de fachada**: leí el código completo de los 18 tests nuevos (11 integración m2-04, 7 buscador m2-05 + 5 unit aristas). Todos assertan estado real de la DB (filas, NULLs, conteos, orden) o comportamiento observable (exit, forma serializada del envelope); ninguno es tautológico. El test de orden por score usa fixture con relevancia asimétrica real, no un assert sobre datos pre-ordenados.
2. **Oráculo circular** (mismo autor, mismo regex en extractor y referencia): es el riesgo real de `wikilinks-ref.py` — comparte el patrón con el engine, así que un regex mal diseñado pasaría el diff en ∅. Está declarado honestamente en brief y docstring («cross-check del PIPELINE ... no del regex»). Lo mitigué con verificación propia fuera del círculo: (a) test negativo mío — corrompí una arista y el diff la cazó (faltan=1 sobran=1); (b) spot-check independiente mío de una nota no incluida en el spot-check del executor (construir-con-llms: 10/10 exacto); (c) confirmación primaria del falso positivo `:space:` en el fichero fuente real. El regex simple tiene exactamente 1 falso positivo conocido en 473 aristas — adjudicado tolerable en §4.4.
3. **Desviaciones no declaradas**: diff completo de ambas ramas leído línea a línea contra reportes/packages. Única discrepancia encontrada: el «conflicto ~nulo» de los packages es en realidad un conflicto trivial real en `lib.rs` (§5) — imprecisión de estimación de merge, no desviación de implementación; no bloquea.
4. **Superficies selladas alteradas**: `--stat` y diff completo — `schema.rs` (DDL §2), `envelope.rs` (v1 gateado en 52e3080), `analyze.py`, `replay.py`, `eval.jsonl`, results históricos de bm, `gold/` e `indexer.rs` (en m2-05): todos intactos en ambas ramas.
5. **Contaminación AGPL**: código original con nombres propios en castellano (aristas/buscador/notas_fts), sin patrones calcados de basic-memory; el acceso a `memory.db`/config no aparece en ninguna de las dos ramas (m2-04 ni lo necesita; m2-05 declara no haberlo tocado y el diff lo confirma — el search no lee config).
6. **Regresiones en suites previas**: cargo test completo re-corrido en ambos worktrees — los tests de M2-03 (indexer/nota/schema/walker/git_epoch/smoke) verdes en las dos ramas; corpus-parity ∅ con la DB de cada rama (la paridad de M2-03 no se rompe desde ninguno de los dos lados).
7. **Artefactos commiteados falsificados** (m2-05): regeneré `engine-fts.jsonl` y `metrics-engine-fts.md` con mi propia corrida — metrics byte-idéntico, jsonl 56/56 exacto módulo `elapsed_s`. Lo commiteado es lo que el código produce.
8. **Contrato §4.1 byte a byte**: cotejé el struct `Busqueda`/`Resultado` contra el JSON de spec §4.1 campo a campo (`query`, `search_type`, `elapsed_s`, `results[{permalink, type, score}]`, `type: "entity"` fijo, orden score DESC, score informativo `-bm25`) y validé el envelope real parseando el stdout de `exo search --json` en mis 56 invocaciones.

Nada de lo encontrado alcanza umbral de rechazo; la única corrección material es la nota de merge de §5.

---

**Veredicto final: `m2-04` MERGED · `m2-05` MERGED** (orden sugerido: 04 primero, 05 después, resolviendo el conflicto trivial de `lib.rs` según §5).
